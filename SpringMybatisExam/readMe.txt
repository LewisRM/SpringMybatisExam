1）数据库配置文件在 src\main\resources
2）题目完成了1,2,3,4，5问，后面的AOP没有时间做了，准别继续做，做完再提交，第九问有级联删除，用mybatis 还不咋会。。。。。
3）感觉这次好难，时间好紧啊。。。

运行代码

mvn clean compile

mvn exec:java -Dexec.mainClass="Main" -Dexec.args="arg0 arg1 arg2"
